# --- Database -------------------------------------------------------------
# Postgres connection string used by drizzle-kit and the runtime.
DATABASE_URL=postgres://user:password@localhost:5432/loa

# --- LLM provider ---------------------------------------------------------
# API key for the LLMProvider implementation.
ANTHROPIC_API_KEY=

# --- Control-plane MCP server ---------------------------------------------
# Port the @loa/mcp server listens on.
MCP_PORT=8787

# --- Cookie vault ---------------------------------------------------------
# Symmetric key used to encrypt per-account session cookies at rest.
# Generate with: openssl rand -base64 32
COOKIE_VAULT_KEY=

# --- Egress proxies -------------------------------------------------------
# Default proxy URL for account runners; per-account bindings override this.
PROXY_URL=
PROXY_USERNAME=
PROXY_PASSWORD=

# --- Runtime --------------------------------------------------------------
# node | development
NODE_ENV=development
# Log level: debug | info | warn | error
LOG_LEVEL=info
